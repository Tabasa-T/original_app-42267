<!DOCTYPE html>
<html>
  <head>
    <title>ときの図書室</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="turbo-cache-control" content="no-cache">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <!-- Google Font: Noto Sans JP（任意） -->
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
      body {
        font-family: 'Noto Sans JP', sans-serif;
        background-color: #fdf6f0;
        color: #333;
        margin: 0;
        padding: 0;
      }
      main {
        max-width: 800px;
        margin: 2rem auto;
        padding: 1rem;
      }
      footer {
        background-color: #f4e3d7;
        color: #6e5a47;
        padding: 1rem 0;
        text-align: center;
        font-size: 0.9rem;
      }
    </style>
  </head>

  <body data-user-id="<%= current_user&.id %>">
    <!-- ヘッダー -->
    <header class="bg-light border-bottom py-3 mb-4">
      <div class="container d-flex justify-content-between align-items-center">
        <strong class="fs-4">ときの図書室</strong>
        <nav>
          <% if user_signed_in? %>
            <%= link_to "ホーム", root_path, class: "btn btn-sm btn-outline-secondary me-2" %>
            <%= link_to "記録する", new_post_path, class: "btn btn-sm btn-outline-success me-2" %>
            <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-sm btn-outline-danger" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "btn btn-sm btn-outline-secondary me-2" %>
            <%= link_to "新規登録", new_user_registration_path, class: "btn btn-sm btn-outline-success" %>
          <% end %>
        </nav>
      </div>
    </header>
    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <!-- オーバーレイ用のHTML（常に含める） -->
    <div id="force-exit-overlay">
      <div class="message">
        あなたの今日の価値作りはここからだね！<br>
        また明日、集中した時間を一緒に作ろう☕️
      </div>
    </div>
    <!-- メイン -->
    <main>
      <%= yield %>
    </main>
    <!-- フッター -->
    <footer>
      ときの図書室 – じかんのあとを大切に  
    </footer>
  </body>
</html>
